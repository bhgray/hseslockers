'use strict';

describe('lockerList module', function() {

  beforeEach(module('lockerList'));

  describe('lockerList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var lockerCtrl = $controller('LockerListCtrl');
      expect(lockerCtrl).toBeDefined();
    }));

  });
});
